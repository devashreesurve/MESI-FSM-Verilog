# MESI-FSM-Verilog
This project implements a MESI (Modified, Exclusive, Shared, Invalid) cache coherence controller using Verilog HDL.  The design models the MESI finite state machine used in multiprocessor cache coherence protocols and demonstrates state transitions for CPU and snoop operations.
